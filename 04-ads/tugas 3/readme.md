```
STORE "kata" with string value
STORE "sambung" = value from kata split string to array
THEN JOIN 

STORE "balik" with sambung
THEN split string to array
REVERSE JOIN
IF BOOLEAN "kata" equals "balik" THEN "true"
ELSE "false"
```

